#!/usr/bin/env python3
"""Module contenant des fonctions de calcul sur des listes de nombres."""

from typing import List


def sum_list(input_list: List[float]) -> float:
    """Retourne la somme de tous les éléments d'une liste de flottants."""
    return sum(input_list)
